{
  "name": "realtime-messaging-app",
  "version": "1.0.0",
  "description": "19th May, 2026",
  "license": "MIT",
  "author": "Charles Daniel",
  "type": "commonjs",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm install --production=false --prefix client && npm install --production=false --prefix backend",
    "build": "npm run build --prefix client && npm run build --prefix backend",
    "start": "cd backend && node dist/index.js"
  }
}
